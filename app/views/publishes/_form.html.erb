<%= form_for(@publish) do |f| %>
  <% if @publish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@publish.errors.count, "error") %> prohibited this publish from being saved:</h2>

      <ul>
      <% @publish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %><%= f.text_field :publishname %>

  <div class="field">
    <%= f.label :publishname %><br>
    
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.select :address, ["島根県","東京都","大阪府"] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
